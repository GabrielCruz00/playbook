// Función que se guardará en este módulo como información 
exports.info = (message) => {
    console.log(`info: ${message}`);
}

//Función que se guardará en este módulo como verbose
exports.verbose = (message) => {
    console.log(`verbose: ${message}`);
}